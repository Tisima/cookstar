<div>
  <h2 class="py-4">本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div class="px-3 py-4">
    <%= link_to :back, class: 'btn btn-primary' do %>
      戻る
    <% end %>

    <%= link_to "退会する", admin_withdraw_user_path(@user.id), method: :patch %>
  </div>
</div>