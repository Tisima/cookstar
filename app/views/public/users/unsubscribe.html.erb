<div>
  <h2>本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div>
    <%= link_to :back do %>
      <button>戻る</button>
    <% end %>
    <%= link_to "退会する", withdraw_user_path(current_user), method: :patch %>
  </div>
</div>