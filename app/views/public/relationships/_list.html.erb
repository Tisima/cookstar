<% if users.exists? %>
  <% users.each do |user| %>
      </thead>
      <tbody>
        <tr>
          <td><%= image_tag user.get_profile_image, size: "100x100"%></td>
          <td><%= user.name %></td>
          <td>フォロー数: <%= user.followings.count %></td>
          <td>フォロワー数: <%= user.followers.count %></td>
          <td><%= link_to "詳細ページへ", user_path(user.id) %></td>
        </tr>
  <% end %>
<% else %>
  <p class="px-4 py-4">ユーザーはいません</p>
<% end %>